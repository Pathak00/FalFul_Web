USE FalFulDb;
GO
SET QUOTED_IDENTIFIER ON; SET ANSI_NULLS ON;
GO
CREATE OR ALTER PROCEDURE sp_PasswordResetToken_MarkUsed
    @Id INT
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE PasswordResetTokens
    SET IsUsed = 1
    WHERE Id = @Id;
END
GO
